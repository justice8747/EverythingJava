server:
  port: 8081

spring:
  application:
    name: order-service

  datasource:
    url: jdbc:mysql://127.0.0.1:3307/order_service
    username: root
    password: mysql

  jpa:
    hibernate:
      ddl-auto: none
